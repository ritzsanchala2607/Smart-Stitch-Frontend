// API Base URL - uses environment variable with fallback to localhost
export const API_URL = "https://smart-stitch-backend.onrender.com" || "http://localhost:8080";

console.log('API Base URL:', API_URL);