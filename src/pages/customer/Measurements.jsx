import PlaceholderPage from '../../components/common/PlaceholderPage';
import { Ruler } from 'lucide-react';

const Measurements = () => {
  return <PlaceholderPage role="customer" title="My Measurements" description="View and manage your measurement profiles" icon={Ruler} />;
};

export default Measurements;
