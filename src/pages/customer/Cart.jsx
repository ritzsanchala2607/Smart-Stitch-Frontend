import PlaceholderPage from '../../components/common/PlaceholderPage';
import { ShoppingCart } from 'lucide-react';

const Cart = () => {
  return <PlaceholderPage role="customer" title="Shopping Cart" description="Review your selected items" icon={ShoppingCart} />;
};

export default Cart;
