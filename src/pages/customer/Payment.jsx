import PlaceholderPage from '../../components/common/PlaceholderPage';
import { CreditCard } from 'lucide-react';

const Payment = () => {
  return <PlaceholderPage role="customer" title="Payment" description="Manage payments and invoices" icon={CreditCard} />;
};

export default Payment;
