import PlaceholderPage from '../../components/common/PlaceholderPage';
import { MessageSquare } from 'lucide-react';

const Chat = () => {
  return <PlaceholderPage role="worker" title="Chat" description="Chat with the owner" icon={MessageSquare} />;
};

export default Chat;
