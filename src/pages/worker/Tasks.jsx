import PlaceholderPage from '../../components/common/PlaceholderPage';
import { ClipboardList } from 'lucide-react';

const Tasks = () => {
  return <PlaceholderPage role="worker" title="My Tasks" description="View and manage your assigned tasks" icon={ClipboardList} />;
};

export default Tasks;
