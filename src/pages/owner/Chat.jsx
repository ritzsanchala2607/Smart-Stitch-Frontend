import PlaceholderPage from '../../components/common/PlaceholderPage';
import { MessageSquare } from 'lucide-react';

const Chat = () => {
  return <PlaceholderPage role="owner" title="Internal Chat" description="Communicate with your workers" icon={MessageSquare} />;
};

export default Chat;
