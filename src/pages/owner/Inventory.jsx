import PlaceholderPage from '../../components/common/PlaceholderPage';
import { Warehouse } from 'lucide-react';

const Inventory = () => {
  return <PlaceholderPage role="owner" title="Inventory Management" description="Manage fabrics, accessories, and stock" icon={Warehouse} />;
};

export default Inventory;
