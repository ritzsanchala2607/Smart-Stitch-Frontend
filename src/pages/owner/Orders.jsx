import PlaceholderPage from '../../components/common/PlaceholderPage';
import { Package } from 'lucide-react';

const Orders = () => {
  return <PlaceholderPage role="owner" title="Order Management" description="Track and manage all orders" icon={Package} />;
};

export default Orders;
